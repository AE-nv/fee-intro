# 3 — Routing and shared state

**Start from** `2-state-management-and-reactivity-solution` · **Solution**
`3-routing-and-shared-state-solution`

The basket works, but there's nowhere to see it.

## What to build

1. Add a `/cart` route showing the basket: one line per sandwich, with a
   quantity stepper and a line total.
2. Show a total, and an empty state when the basket is empty.
3. Link the header's cart icon to `/cart`, and mark the active route.
